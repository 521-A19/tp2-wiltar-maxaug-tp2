<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="TP2.Views.DogsListPage"
             Title="{Binding Title}">
  <ContentPage.Content>

        <StackLayout>
            <Button FontSize="Medium" Text="Ajouter un chien" Command="{Binding GoToDogShopCommand}"/>
            
            <Label HorizontalOptions="Center" Text="Voici les petits amis en adoption" FontSize="Large"></Label>
            <ListView x:Name ="dogsListView" 
                      ItemsSource="{Binding Dogs}"
                      SelectedItem="{Binding SelectedDog}"
                      HasUnevenRows="True"
                      AutomationId="ListView">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <StackLayout Padding="10">
                                <Image Source="{Binding ImageUrl}" HeightRequest="96" WidthRequest="120"></Image>
                                <Label Text="{Binding Name}"></Label>
                                <Label Text="{Binding Race}"></Label>
                                <Label Text="{Binding Sex}"></Label>
                                <Label Text="{Binding Price}"></Label>
                                <Label Text="{Binding Description}"></Label>
                            </StackLayout>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>

            </ListView>
            <StackLayout HorizontalOptions="Start">
                <Button Text="Trier"></Button>
                <Button Text="Rechercher"></Button>
            </StackLayout>
        </StackLayout>
    </ContentPage.Content>
</ContentPage>